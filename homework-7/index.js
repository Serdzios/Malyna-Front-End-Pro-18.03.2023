const num1 = Number(prompt("Введіть перше число:"));
const num2 = Number(prompt("Введіть друге число:"));
const num3 = Number(prompt("Введіть третє число:"));

const avg = (num1 + num2 + num3) / 3;

alert(`Середнє арифметичне: ${avg}`);
